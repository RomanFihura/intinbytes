#include "flaviy.h"

#include <iostream>

int main(int argc, char *argv[])
{
    // std::cout << flaviy(12) << std::endl;
    // auto head = generate_list(10);
    // auto current = head;
    // do
    // {
    //     std::cout << current->element << std::endl;
    //     current = current->p_next;
    // } while (current != head);

    // std::cout << flaviy(5) << std::endl;
    // std::cout << flaviy_list(5) << std::endl;

    return 0;
}